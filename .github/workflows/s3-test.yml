# New Static file test
name: Deploy a Static file in S3
on:
 push
jobs:
  build:
    runs-on:
      ubuntu-22.04
    steps:
      - name: Checkout code from repo
        uses: actions/checkout@v4
      - name: Configure AWS Creds
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - name: Code static website
        run: |
          aws s3 sync ./ s3://ga-s3-nox \
          --exclude "*" \
          --include "index.html"
          


# # This is a basic workflow to help you get started with Actions
# name: Deploy Static from S3 # name is Learn
# on: # how it executes; manually here
#   push:
# jobs: 
#   deploy:
#     runs-on: 
#       ubuntu-latest
#     steps:
#       - name: Checkout code from the repo
#         uses: actions/checkout@v4
#       - name: Configure AWS credentials to interact with S3 
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1
#       - name: Copy index.html to S3 bucket
#         run: |
#           aws s3 sync ./ s3://nox-s3-ga/ \
#           --exclude "*" \
#           --include "index.html"
          

          
      # - name: Checkout the code
      #   uses: actions/checkout@v4
      # - name: Print index.html
      #   run: cat index.html
      
  #   needs: trial
  #   steps:
  #     - name: First Push
  #       run: |
  #         echo "Testing push trigger in GA😎"
  #         echo "Xup, ${{ vars.TEST_NAME }}!"
  
  # trial:
  #   runs-on: ubuntu-latest
    
  #   steps:
  #     - name: Create directories
  #       run: |
  #         mkdir test
  #         cd test     
  #         touch t.txt b.sh     
  #         echo "I'm in $PWD." > b.sh
  #         cat b.sh
  #         echo "I created directories."
  #         ls -la

# jobs:  # job name: test 
#   test:
#     runs-on: ubuntu-latest   # where it is executed; os it runs on
#     # needs: trial # runs after the trial job completes
#     steps:
#       - name: First test
#         run: 
#           echo "Testing GitHub Actions."
#       - name: Multi-line
#         run: |
#           echo "Try other stuff."
#           echo "Testing stuff 🚀🚀🚀"
#           echo "Ran trial prior to this"
#           echo "Now we are talking 🚀🚀🚀"
  # trial:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Create directories
  #       run: |
  #         mkdir test
  #         cd test     
  #         touch t.txt b.sh     
  #         echo "I'm in $PWD." > b.sh
  #         cat b.sh
  #         echo "I created directories."
  #         ls -la
        
      
    
